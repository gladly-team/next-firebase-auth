// eslint-disable-next-line
const setAuthCookies = async (req) => {
  // TODO
  return {
    idToken: 'abc',
    refreshToken: 'def',
    AuthUser: { foo: 'bar' },
  }
}

export default setAuthCookies
